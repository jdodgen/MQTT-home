alertaway.com server notes
as root

cd /var/www
chown -R www-data:www_data
chmod -R g+rw

addusr  alertaway   password foobar
usermod -a -G www-data jim

###  not used -- usermod -d /var/www/alertaway.com/ftp

cpan Image::Magik
cpan DBI
cpan DBD::SQLite
cpan Storable;

backup stuff
make a copy runs forever >12 hours?
dd -if /dev/sda -of /dev/sdb

mkdir /mnt/remdrive
vi /etc/fstab
add
/dev/sdb1       /mnt/remdrive   ext4    defaults        0       2

/usr/bin/xxync -navl -delete /var/www /mnt/remdrive/var/www

rsync -naAX / --delete --exclude={"/dev/*","/proc/*","/sys/*","/tmp/*","/run/*","/mnt/*","/media/*","/lost+found"} /mnt/remdrive


remove the n when not testing



a2ensite example.com.conf
service apache2 restart









